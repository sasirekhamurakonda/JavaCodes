package BasicsOfJava;

/*
class Box {
    double width;
    double height;
    double depth;
    Box() {
        System.out.println("Constructing Box");
        width = 10;
        height = 10;
        depth = 10;
    }
    double volume() {
        return width * height * depth;
    }


    public boolean equals(Box obj) {
       return  this.width==obj.width&&this.height==obj.height&&this.depth==this.depth;
    }
}
// This class declares an object of type Box.
class SimpleClass {
    public static void main(String args[]) {

        Box mybox1 = new Box();
        Box mybox2 = new Box();
        double vol;
// get volume of first box
        vol = mybox1.volume();
        System.out.println("Volume is " + vol);
// get volume of second box
        vol = mybox2.volume();
        System.out.println("Volume is " + vol);
        if(mybox1.equals(mybox2))
            System.out.println("We are same");
        else
            System.out.println("We are not same");
    }
}
*/
